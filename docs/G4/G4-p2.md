Report the similarities and discrepancies between the tools.
Are there errors/warnings that one tool reports but the other doesn't (give at least 3 examples, if available)?
Are there error/warnings that all three tools report? (give at least 3 examples, if available)

## Errors one tool reports but not the other

- `TartanHomeService.toIoT___`has Boolean return type and returns explicit null
    - SpotBugs::NP_BOOLEAN_RETURN_NULL
    - PMD and ErrorProne both don't report anything

- `TartanHomeConnectException` and `LoginAttemptsExceededException` both implement `Serializable` but don't set a
  serialVersionUID
    - PMD::MissingSerialVersionUID
    - SpotBugs and ErrorProne both don't report anything

- `IoTControlManager` has an empty `@param` tag on `processStateUpdate()`
    - ErrorProne::EmptyBlockTag
    - SpotBugs and PMD both don't report anything

## Errors all three tools report

`TartanHome` declares `equals()` but not `hashCode()`

- SpotBugs::HE_EQUALS_USE_HASHCODE (equals() is defined but hashCode() isn't)
- PMD::OverrideBothEqualsAndHashcode (Ensure you override both equals() and hashCode())
- ErrorProne::EqualsHashCode

There is an empty catch block in `IoTControlManager` (not valid example, but can be talked about)

- SpotBugs: Nothing :(
- PMD::Avoid Empty Catch Blocks
- ErrorProne::EmptyCatch

`IoTControlManager` might not close OS resources (not valid example, but can be talked about)

- SpotBugs::OS_OPEN_STREAM (Method may fail to close stream)
- PMD::CloseResource (Ensure that resources are closed after use)
- ErrorProne: Nothing :(
